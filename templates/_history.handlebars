{{#talks}}
<article class="history-event">
  <header class="speaker">
    {{#if speaker}}
      {{#if speaker.portrait}}
         <div class="speaker-portrait" style="background-image: url({{speaker.portrait}});"></div>
      {{else }}
        <span class="fa-stack fa-5x">
          <i class="fa fa-question fa-stack-1x"></i>
        </span>
      {{/if}}
      <span class="speaker-name"><a href="http://twitter.com/{{speaker.twitter}}">{{speaker.name}}</a></span>
    {{else }}
      <span class="fa-stack fa-5x">
        <i class="fa fa-square-o fa-stack-2x"></i>
        <i class="fa fa-question fa-stack-1x"></i>
      </span>
    {{/if}}
  </header>
  <div class="event-content">
    {{#if title}}
      <div class="meta">
        Language: {{language}} | Level: {{level}}
      </div>
      <h3>{{title}}</h3>
      <p>{{description}}</p>
      <div class="resources">
        {{#if git}}
          <a href="{{git}}"><i class="fa fa-github fa-2x"></i></a>
        {{/if}}
        {{#if slides}}
          <a href="{{slides}}"><i class="fa fa-desktop fa-2x"></i></a>
        {{/if}}
        {{#if video}}
          <a href="{{video}}"><i class="fa fa-vimeo-square fa-2x"></i></a>
        {{/if}}
        {{#if blog}}
          <a href="{{blog}}"><i class="fa fa-rss-square fa-2x"></i></a>
        {{/if}}
      </div>
    {{/if}}
  </div>
</article>
{{/talks}}
